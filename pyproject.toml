[project]
name = "sbaquacontrol"
version = "0.5.001"
description = "ESP8266-based aquarium LED light controller with interactive web interface"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["flask", "flask-cors"]

[dependency-groups]
dev = ["platformio", "pytest", "requests"]
